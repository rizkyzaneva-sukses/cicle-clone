node_modules
.env
.env.local
dist
.DS_Store
*.log
src/public/uploads/*
!src/public/uploads/.gitkeep

# Keep migrations committed for production deploys (prisma migrate deploy)
# prisma/migrations
# (uncomment above line only if you want to ignore migration history)
